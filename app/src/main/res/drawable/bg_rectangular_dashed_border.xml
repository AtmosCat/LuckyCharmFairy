<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="#FFFFFF" /> <!-- 배경 색상 설정 (필요에 따라 변경) -->
    <stroke
        android:color="@color/main_medium_gray"
        android:width="0.7dp"
        android:dashWidth="5dp"
        android:dashGap="2dp"
        />
</shape>